// Clase 11 - 9/2/21

// JQuery

// Ya no se usa tanto pero era la posta hasta hace poco.

// Lo llamo desde HTML con href.

// El archivo viene MINIFICADO, que es cuando te viene sin espacios todo comprimido.
// A los MINIFICADOS les suelen poner loquesea.min.js

// JQuery siempre se enuncia con $
// puedo hacerlo todo adentro de un jquery(window).load(function())

// Esto en Jquery te avisa cuándo se terminó de cargar el html
// $( document ).ready(function())

// Esta hace lo mismo pero también contempla que estén todas las imágenes cargadas
// $( window ).load(function())

// document.getElementById("productos").innerhtml = aux;
// se reemplaza por:
// $("#productos").html(aux);

// # porque es ID. Se puede con o sin # pero hacen cosas distintas.

